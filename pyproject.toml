[project]
name = "needle"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "huggingface_hub",
    "transformers",
    "sentencepiece",
    "tqdm",
]

[project.optional-dependencies]
mlx = ["mlx-vlm>=0.5"]
gemma-ft = ["mlx-lm>=0.25", "pyyaml"]
train = [
    "jax",
    "jaxlib",
    "flax",
    "optax",
    "datasets",
    "wandb",
    "scipy",
    "gcsfs",
    "google-genai",
]
tpu = ["jax[tpu]"]
gpu = ["jax[cuda12]"]

[project.scripts]
needle = "needle.cli:main"

[tool.setuptools.packages.find]
include = ["needle*"]

[build-system]
requires = ["setuptools>=68.0"]
build-backend = "setuptools.build_meta"
